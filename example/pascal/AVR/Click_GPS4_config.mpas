unit Click_GPS4_config;

uses Click_GPS4_types;

const
    _GPS4_TIMER_LIMIT : uint32_t = 5; // 5 ticks
    _GPS4_BUF_WARNING : uint16_t = 192; // 192 bytes activate warning
    _GPS4_POLL_ENABLE : uint8_t = 1; // poll enabled
    _GPS4_CALLBACK_ENABLE : uint8_t = 0; // calback disabled
    _GPS4_UART_CFG : array[3] of uint32_t = (9600, _UART_NOPARITY, _UART_ONE_STOPBIT); 
    
end.